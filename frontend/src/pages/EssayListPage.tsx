import PlaceholderPage from "../components/PlaceholderPage";

/** 任务状态看板（识别中 / 待校对 / 已定稿，T03 实现）。 */
export default function EssayListPage() {
  return (
    <PlaceholderPage
      title="作文状态看板"
      description="展示全班作文的识别进度与校对状态，点击进入逐句校对。"
    />
  );
}
