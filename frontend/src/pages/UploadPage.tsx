import PlaceholderPage from "../components/PlaceholderPage";

/** 拍照 / 选图多张上传（手机 capture，T03 实现）。 */
export default function UploadPage() {
  return (
    <PlaceholderPage
      title="上传作文原片"
      description="手机浏览器/微信内调用相机拍照，支持多张上传并自动投递识别任务。"
    />
  );
}
