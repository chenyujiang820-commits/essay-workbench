import PlaceholderPage from "../components/PlaceholderPage";

/** 期数列表 + 新建一期（T03 实现）。 */
export default function IssuePage() {
  return (
    <PlaceholderPage
      title="期数列表"
      description="按周列出每一期，支持新建一期并进入拍照上传 / 状态看板。"
    />
  );
}
